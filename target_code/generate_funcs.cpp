#include "tcode_generator.h"
#include "../common_interface/vm_types.h"

void generate(vector<intermediate_code::quad*> quad_vector){
	for (intermediate_code::quad const *q : quad_vector){
		switch (q->opcode)
		{
		default:
			break;
		}
	}
}

/*simplirosi generate functions*/